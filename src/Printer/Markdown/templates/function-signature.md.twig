{{ class.fqsen }}::{{ function.name }}( {% for argument in function.arguments %}
{{- argument.type ? argument.type~' ' }}
{{- argument.byReference ? '&' }}
{{- argument.name }}{{ argument.default ? ' = '~argument.default|raw }}
{%- if not loop.last %}, {% endif %}
{%- endfor %} )
{{- function.returnType ? ': '~function.returnType :  ': '~function.docblock.tagsByName('return')|first.type }}