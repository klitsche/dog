### {{ method.name }}

{#**Description**#}
```php

{% include 'method-signature.md.twig' %}

```

{{ method.docblock.summary|raw }}

{{ method.docblock.description|raw }}

{% if method.docblock.tagsByName('deprecated') is not empty  %}⚠️ _This method is **deprecated**._ _{{ method.docblock.tagsByName('deprecated')|first.description }}_
{% endif %}

{% if method.docblock.tagsByName('see') is not empty or method.docblock.tagsByName('link') is not empty %}
See also:

{% for see in method.docblock.tagsByName('see') ~%}
* {{ linkReference(see.reference) }}
{%- endfor %}
{% for link in method.docblock.tagsByName('link') ~%}
* [{% if link.description and link.description != link.link %}{{ link.description|raw }}{% else %} {{ link.reference }} {% endif %}]({{ link.link }})
{%- endfor %}

{% endif %}

{% if method.arguments is not empty %}
{#**Parameters**#}

| Parameter | Type | Description |
|-----------|------|-------------|
{% for argument in method.arguments %}
| `{{ argument.name }}` | **{{ argument.type|raw }}** | {{ argument.description|split('\n')|join(' ')|raw }} |
{% endfor %}

{% endif %}{# method.arguments is not empty #}

{% if method.returnType and method.returnType != 'void' %}
{#**Return Values**#}

Returns `{{ method.returnType }}`. {{ method.returnDescription }}
{% endif %}
